import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Rien de spécial : le proxy backend est géré par
  // app/backend/[...path]/route.ts (API Route Next.js)
};

export default nextConfig;
